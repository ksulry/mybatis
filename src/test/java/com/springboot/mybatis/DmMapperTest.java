/*
package com.springboot.mybatis;

import com.alibaba.fastjson.JSON;
import com.sinog2c.workbench.model.Person;
import com.springboot.mybatis.mapper.dm.CommDeptMapper;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

*/
/**
 * @author ln
 * @Description: ${todo}
 * @date 2020/8/26  17:52
 *//*

@SpringBootTest(classes = MybatisApplication.class)
@RunWith(SpringRunner.class)
public class DmMapperTest {
    @Autowired
    private CommDeptMapper commDeptMapper;

    @Test
    public void testCollectionMapper(){
        Person stringListMap = commDeptMapper.selectDeptList01("1100");
        String json = JSON.toJSONString(stringListMap);
        System.out.println("打印json数据:" + json);
    }
}
*/
